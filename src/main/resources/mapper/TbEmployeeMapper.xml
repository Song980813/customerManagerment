<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huayu.management.mapper.TbEmployeeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.huayu.management.entity.TbEmployee">
        <id column="id" property="id" />
        <result column="usernum" property="usernum" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="sex" property="sex" />
        <result column="age" property="age" />
        <result column="post_id" property="postId" />
        <result column="phone" property="phone" />
        <result column="identification_photo" property="identificationPhoto" />
        <result column="motto" property="motto" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, usernum, password, name, sex, age, post_id, phone, identification_photo, motto
    </sql>

</mapper>
